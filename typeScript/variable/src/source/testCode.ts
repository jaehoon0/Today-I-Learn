//let
let variable: number = 10

const variable2: string = 'hi'

let a: any = 0
// variable = true  불가능
a = true // 가능

let u: undefined = undefined // undefined는 타입과 값이 모두 가능

export function templateString() {
	let v1 = 'hello'
	console.log(`${v1} 안뇽`)
}